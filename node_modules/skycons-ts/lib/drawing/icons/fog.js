"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var constants_1 = require("../../constants");
var fogbank_1 = require("../elements/fogbank");
var line_1 = require("../elements/line");
function iconFog(ctx, time) {
    var width = ctx.canvas.width;
    var height = ctx.canvas.height;
    var shorter = Math.min(width, height);
    var strokeWidth = shorter * constants_1.STROKE;
    fogbank_1.fogbank(ctx, time, width * 0.5, height * 0.32, shorter * 0.75, strokeWidth);
    time /= 5000;
    ctx.lineWidth = strokeWidth;
    var gapBottom = height * 0.936;
    var top1 = Math.floor(gapBottom - strokeWidth * 2.5) + 0.5;
    var gapLeft1 = Math.cos((time + 0.5) * constants_1.TAU) * shorter * 0.02;
    var gapRight1 = Math.cos((time + 0.75) * constants_1.TAU) * shorter * 0.02;
    line_1.line(ctx, gapLeft1 + width * 0.2 + strokeWidth * 0.5, top1, gapRight1 + width * 0.8 - strokeWidth * 0.5, top1);
    var top2 = Math.floor(gapBottom - strokeWidth * 0.5) + 0.5;
    var gapLeft2 = Math.cos(time * constants_1.TAU) * shorter * 0.02;
    var gapRight2 = Math.cos((time + 0.25) * constants_1.TAU) * shorter * 0.02;
    line_1.line(ctx, gapLeft2 + width * 0.2 + strokeWidth * 0.5, top2, gapRight2 + width * 0.8 - strokeWidth * 0.5, top2);
}
exports.iconFog = iconFog;
